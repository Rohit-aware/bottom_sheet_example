export interface BottomSheetTheme {
  colors: {
    background: string;
    backdrop: string;
    handle: string;
  };
  radius: {
    container: number;
    handle: number;
  };
  spacing: {
    handleMarginVertical: number;
  };
  sizing: {
    handleWidth: number;
    handleHeight: number;
  };
}
